package dev.solsynth.solian.theme

import androidx.compose.ui.graphics.Color

// ── OLED-optimized blacks ──
val OledBlack = Color(0xFF000000)
val OledSurface = Color(0xFF0D0D0D)
val OledSurfaceVariant = Color(0xFF1A1A1A)

// ── Brand / Accent ──
val SolianViolet = Color(0xFF7D80BA)
val SolianVioletBright = Color(0xFF9DA0D4)
val SolianVioletDim = Color(0xFF5A5D8A)

// ── Content ──
val OnSurfaceHigh = Color(0xFFF0F0F0)
val OnSurfaceMedium = Color(0xFFB0B0B0)
val OnSurfaceLow = Color(0xFF6A6A6A)

// ── Semantic ──
val ErrorRed = Color(0xFFFF5252)
val SuccessGreen = Color(0xFF4CAF50)
val WarningAmber = Color(0xFFFFC107)
